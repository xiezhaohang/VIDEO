{
  "name": "video-director-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first control plane for AI Product Video Director",
  "scripts": {
    "dev": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=20" }
}
